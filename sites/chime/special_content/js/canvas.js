const d = document;
let WIDTH, HEIGHT, canvas, context, circles;

function canvasload() {
    WIDTH = d.querySelector('#result_canvas').getAttribute('width');
    HEIGHT = d.querySelector('#result_canvas').getAttribute('height');

    
};